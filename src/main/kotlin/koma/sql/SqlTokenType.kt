package koma.sql

enum class SqlTokenType {
    QUOTE,
    OPEN_BRACKET,
    CLOSE_BRACKET,
    SINGLE_LINE_COMMENT,
    MULTI_LINE_COMMENT,
    BIND_VALUE_DIRECTIVE,
    LITERAL_VALUE_DIRECTIVE,
    EMBEDDED_VALUE_DIRECTIVE,
    IF_DIRECTIVE,
    ELSEIF_DIRECTIVE,
    ELSE_DIRECTIVE,
    FOR_DIRECTIVE,
    EXPAND_DIRECTIVE,
    END_DIRECTIVE,
    DELIMITER,
    SELECT,
    WHERE,
    FROM,
    GROUP_BY,
    HAVING,
    ORDER_BY,
    FOR_UPDATE,
    OPTION,
    AND,
    OR,
    UNION,
    MINUS,
    EXCEPT,
    INTERSECT,
    WORD,
    OTHER,
    WHITESPACE,
    EOL,
    EOF;
}
